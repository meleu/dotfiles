#!/usr/bin/env bash

echo '
TODO:
=====

- list of files to ignore
- go through all files
- check if the respective file exists in "$HOME"
  - if it is a symbolic link to the same file, skip it
  - save a backup
  - remove the file
- create a symbolic link
'
